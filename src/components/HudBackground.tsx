/**
 * TERMINAL backdrop (rebuilt 2026-07-03, founder vision): the room is BLACK.
 * The old HUD layers (vignette, perspective grid floor, scan sweep, corner
 * brackets) are gone — the CRT scanline lives on <body> in globals.css, and
 * content panes carry all the structure. This component stays as the seam in
 * case a backdrop ever returns.
 */
export default function HudBackground() {
  return null;
}
