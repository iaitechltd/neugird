# kit@7 vs @solana-program/*'s ^6 peer ranges — the tree works at runtime
# (devnet-verified: SAS mint/revoke + x402 signing). Without this, `npm ci`
# (incl. the Docker deps stage) refuses the lockfile.
legacy-peer-deps=true
